    {
      {int cont = 1;
    int Num, maiorNum; 

    maiorNum = 0;
   
    
    while (cont <= 5){
      System.out.println("Digite o "+ cont + "º Número");
      Num = teclado.nextInt();

      if(Num > maiorNum){
        maiorNum = Num ;
      }

      cont++;
    }

    System.out.println("O maior número digitado foi "+ maiorNum);
  }
  
  }